# Environment handoff

The maintained handoff is
[`docs/CLUSTER_RL_RUNBOOK.md`](docs/CLUSTER_RL_RUNBOOK.md). It covers reusable
venvs, cluster paths, validation and launch commands, the Verifiers environment
pattern, and the PRIME/Qwen fixes discovered during the pilot.
